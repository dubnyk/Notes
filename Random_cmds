#Averages the sum
awk '{sum += $1; count++} END {if (count > 0) print "Average:", sum/count; else print "No records found"}'
#SETUID (run files with elevated privileges)
find / -perm -4000 -type f 2>/dev/null
